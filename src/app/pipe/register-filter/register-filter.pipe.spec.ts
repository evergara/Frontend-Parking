import { RegisterFilterPipe } from './register-filter.pipe';

describe('RegisterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RegisterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
